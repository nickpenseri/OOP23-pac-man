package it.unibo.model.map.api;

/**interface that give the random map name. */
public interface MapSelector {
    /**
     * return random map name.
     * @return map name
     */
    String getMapName();
}

package it.unibo.model.ghost.api.ghostBehaviour;

public enum GhostBehaviours {
    
    /**
     * a normal behaviour for a ghost, it follow the target for some time and go aorund aat random for the same time.
     */
    NORMAL, 
    
    /**
     * a behaviour for a ghost that follow always the target.
     */
    AGGRESSIVE
}

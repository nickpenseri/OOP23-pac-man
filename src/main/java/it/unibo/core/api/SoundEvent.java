package it.unibo.core.api;

/**enumeration to differentiate the events that generate sounds. */
public enum SoundEvent {
    /**life event lose. */
    DEATH,
    /**take bonus event. */
    BONUS,
    /**pac-man movement event. */
    PACMAN;
}

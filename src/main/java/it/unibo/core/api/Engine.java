package it.unibo.core.api;


/** Main interface that run the entire Game. */
public interface Engine {

    /** Manage the controllers of the scenes and the window os the game. */
    void mainLoop();
}
